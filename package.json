{
  "name": "crashpad",
  "version": "0.1.0",
  "description": "DeFi agent safety rails — simulate-before-execute guardrails for autonomous agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "crashpad": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["defi", "safety", "guardrails", "simulation", "mev", "slippage", "agent"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0",
    "viem": "^2.21.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
